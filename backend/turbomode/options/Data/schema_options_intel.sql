CREATE TABLE IF NOT EXISTS enriched_options_signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT,
    base_signal TEXT,
    model_confidence REAL,
    model_probability REAL,
    current_price REAL,
    stop_upper REAL,
    stop_lower REAL,
    prob_buy REAL,
    prob_sell REAL,
    prob_hold REAL,
    created_at TEXT,
    sector TEXT,
    short_call_strike REAL,
    short_put_strike REAL,
    expiration TEXT,
    max_credit REAL,
    max_profit REAL,
    max_loss REAL,
    pnl REAL,
    drift REAL,
    pressure_index REAL,
    volatility_regime TEXT,
    quality_score REAL,
    regime_state TEXT,
    transition_state TEXT,
    transition_strength REAL,
    age_days INTEGER,
    snapshot_timestamp TEXT,
    chain_calls_count INTEGER,
    chain_puts_count INTEGER,
    bid_ask_spread REAL,
    iv_rank REAL,
    iv_percentile REAL,
    delta REAL,
    gamma REAL,
    theta REAL,
    vega REAL,
    rho REAL,
    liquidity_score REAL,
    data_source TEXT,
    scan_batch_id TEXT,
    signal_quality_score REAL,
    signal_strength TEXT,
    confirmation_status TEXT,
    UNIQUE(symbol)
);
